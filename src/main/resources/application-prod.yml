server:
  port: 8080

spring:
  application:
    name: cobrother-dev   # 🔥 override

  datasource:
    url: jdbc:postgresql://localhost:5432/cobrother
    username: postgres
    password: Aultum307
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true